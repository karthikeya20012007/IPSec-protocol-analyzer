[pytest]
markers =
    integration: marks tests as integration tests
    requires_root: marks tests that require root privileges
    requires_ipsec: marks tests that require strongswan/ipsec dependencies
